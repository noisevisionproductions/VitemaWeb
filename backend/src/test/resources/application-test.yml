spring:
  main:
    allow-bean-definition-overriding: true
  security:
    filter:
      order: 10

  mail:
    host: localhost
    port: 3025
    username: test@vitema.pl
    password: test
    properties:
      mail:
        smtp:
          auth: false

app:
  frontend:
    url: https://test-vitema.pl

logging:
  level:
    org.springframework.security: DEBUG
    com.noisevisionsoftware.vitema: DEBUG

