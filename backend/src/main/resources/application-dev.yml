server:
  port: 8080

spring:
  # PostgreSQL Development Configuration
  datasource:
    url: jdbc:postgresql://localhost:5432/vitema
    username: postgres
    password: ${POSTGRES_PASSWORD_DEV}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5

  # JPA Development Configuration
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: smtp-relay.brevo.com
    port: 587
    username: ${BRAVO_LOGIN}
    password: ${BRAVO_KEY}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

firebase:
  storage:
    bucket-name: fitapplication-3a2ae.firebasestorage.app
  service-account:
    path: firebase-service-account.json

cors:
  allowed-origins: http://localhost:5173

app:
  frontend-url: http://localhost:5173
  admin-email: ${ADMIN_EMAIL}

logging:
  level:
    com.noisevisionsoftware.vitema: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE